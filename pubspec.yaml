name: pi_ede_ui
description: "Pi-EDE"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image: ^4.1.7
  logging: ^1.2.0
  network_info_plus: ^5.0.0
  qr_flutter: ^4.1.0
  rdflib: ^0.2.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
